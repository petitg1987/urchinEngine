#include <system/monitor/MonitorResolution.h>

namespace urchin {

    MonitorResolution::MonitorResolution(unsigned int width, unsigned int height, unsigned int rate) :
            width(width),
            height(height),
            rate(rate) {

    }

}
