#pragma once

namespace urchin {

    /**
    * Represents a convex shape.
    * A shape doesn't have a position in space.
    */
    class ConvexShape2D {
        public:
            virtual ~ConvexShape2D() = default;
    };

}
