#pragma once

namespace urchin {

    enum ShapeType {
        TRIANGLE,
        TRIANGLE_STRIP,
        LINE, //TODO remove if not used anymore
        LINE_STRIP, //TODO remove if not used anymore
        POINT
    };

}
